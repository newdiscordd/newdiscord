{
  "name": "discordia",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "npx prisma generate && next build",
    "start": "next start",
    "lint": "next lint",
    "postinstall": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "concurrently": "^8.2.0"
  }
}
